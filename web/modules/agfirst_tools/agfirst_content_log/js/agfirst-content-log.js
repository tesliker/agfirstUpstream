(function($, Drupal, drupalSettings) {
  Drupal.behaviors.AgFirstContentLogScripts = {
    attach: function (context) {

      // Custom JS goes here.

    }
  };

})(jQuery, Drupal, drupalSettings);
